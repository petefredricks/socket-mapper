{
	"name": "socket-mapper",
	"description": "This module is the connection between sockets and redis.",
	"version": "0.0.4",

	"main": "index.js",
	"private": true,

	"dependencies": {
		"connect": "2.2.2",
		"redis": "~0",
		"socket.io": "~0"
	}
}
